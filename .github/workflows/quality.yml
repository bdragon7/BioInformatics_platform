name: Quality Checks

on:
  pull_request:
  push:
    branches: ["main", "work"]

jobs:
  lint-type-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install package + dev extras
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Ruff
        run: ruff check src tests

      - name: MyPy
        run: mypy src

      - name: Pytest with coverage
        run: pytest --cov=src/bioplatform --cov-report=term-missing
